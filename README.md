# Repo is create for Google IT Automation with Python - Automating Real World Tasks with Python

Second exercise is to create python script file_parser.py to parse text file and reformat the file contents to dictionary as part of preparation to POST the contents as feedback to Django web service.  Sample data files are located in data/feedback folder.  

First exercise is to scale and convert images using PIL.  image_scaling.py contains scripts that will iterate through each file in the images folder
For each file:
    - Rotate the image 90° clockwise
    - Resize the image from 192x192 to 128x128
     -Save the image to a new folder in .jpeg format


